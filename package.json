{
  "name": "meting-backend-js",
  "version": "1.1.2",
  "description": "统一的音乐API服务，专为EdgeOne Pages部署优化",
  "main": "app.js",
  "scripts": {
    "build": "node esbuild.config.js",
    "test": "vitest run",
    "patch": "npm version patch && git push origin main && git push origin --tags",
    "minor": "npm version minor && git push origin main && git push origin --tags",
    "major": "npm version major && git push origin main && git push origin --tags"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "type": "module",
  "author": "xizeyoupan",
  "license": "MIT",
  "devDependencies": {
    "vitest": "^0.29.2"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "crypto-browserify": "^3.12.1",
    "hono": "^4.1.0",
    "nanoid": "^4.0.0"
  }
}
